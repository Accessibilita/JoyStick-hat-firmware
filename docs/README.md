# Documentation index

Start with these files before diving into phase-specific notes.

- `../README.md` — repository orientation and current branch warning
- `BRANCH_MODEL.md` — what `main`, `edge`, and `experimental` mean
- `ROADMAP.md` — completed software phases and upcoming hardware gates
- `VALIDATION_LEVELS.md` — exact meaning of implemented/software-validated/target-built/hardware-validated
- `CODING_STANDARD.md` — GhostPCB embedded-firmware rules
- `LICENSING.md` — MPL-2.0 project-owned scope and third-party boundary

On `edge` and `experimental`, the firmware project also carries detailed engineering records under:

```text
src/JoyStick_V2_FreeRTOS/docs/
```

`experimental` contains the most complete phase-specific documentation because it carries the Phase-1 through Phase-5 software architecture.
